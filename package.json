{
  "private": true,
  "workspaces": [
    "windwatts-ui",
    "windwatts-api",
    "windwatts-ssr"
  ],
  "scripts": {
    "prepare": "husky",
    "lint": "eslint",
    "format": "prettier",
    "dev:ssr": "docker compose up --build windwatts-ssr"
  },
  "lint-staged": {
    "windwatts-ui/src/**/*.{js,jsx,ts,tsx}": [
      "./check-format.sh"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.2"
  }
}
